<h1><%= @cocktail.name %></h1>

<ul>
  <% @doses.each do |dose| %>
  <li><%= dose.description %> <%= link_to "Delete dose", dose_path(dose), method: :delete %></li>
  <% end %>
</ul>

<br>
<%= link_to "Add a dose", new_cocktail_dose_path(@cocktail) %>
<br>
<%= link_to "Back to all cocktails", cocktails_path %>
