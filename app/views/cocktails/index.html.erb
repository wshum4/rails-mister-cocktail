<h1>All Cocktails</h1>

<ul>
  <% @cocktails.each do |cocktail| %>
  <li><%= cl_image_tag @cocktail.photo.key, height: 300, width: 400, crop: :fill %> <%= link_to cocktail.name, cocktail_path(cocktail) %></li>
  <% end %>
</ul>

<%= link_to "Add Cocktail", new_cocktail_path %>
